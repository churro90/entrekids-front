import { AutocompleteComunasDirective } from './autocomplete-comunas.directive';

describe('AutocompleteComunasDirective', () => {
  it('should create an instance', () => {
    const directive = new AutocompleteComunasDirective();
    expect(directive).toBeTruthy();
  });
});
