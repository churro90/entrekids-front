import { GeneralPipesPipe } from './general-pipes.pipe';

describe('GeneralPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new GeneralPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
