import { FiltroComunaPipe } from './filtro-comuna.pipe';

describe('FiltroComunaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroComunaPipe();
    expect(pipe).toBeTruthy();
  });
});
