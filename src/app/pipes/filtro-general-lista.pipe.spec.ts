import { FiltroGeneralListaPipe } from './filtro-general-lista.pipe';

describe('FiltroGeneralListaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroGeneralListaPipe();
    expect(pipe).toBeTruthy();
  });
});
