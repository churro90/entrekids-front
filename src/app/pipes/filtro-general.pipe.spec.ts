import { FiltroGeneralPipe } from './filtro-general.pipe';

describe('FiltroGeneralPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroGeneralPipe();
    expect(pipe).toBeTruthy();
  });
});
