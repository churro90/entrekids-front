import { HorariosFilterPipe } from './horarios-filter.pipe';

describe('HorariosFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HorariosFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
